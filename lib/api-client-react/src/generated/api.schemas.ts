/**
 * Generated by orval v8.5.3 🍺
 * Do not edit manually.
 * Api
 * MOTHERSHIP Multi-Agent Governance API
 * OpenAPI spec version: 0.1.0
 */
export type LedgerEntryAction =
  (typeof LedgerEntryAction)[keyof typeof LedgerEntryAction];

export const LedgerEntryAction = {
  submit: "submit",
  fill: "fill",
  cancel: "cancel",
  kill: "kill",
  close: "close",
  error: "error",
} as const;

export type LedgerEntryVerdictRef = {
  stance: string;
  symbol?: string | null;
  bias?: string | null;
  cycle?: number | null;
  /** Reasoning-packet row ID of the Queen FINAL_VERDICT that authorised this execution. */
  packetId?: number | null;
} | null;

/**
 * One link in the SHA-256 hash chain. `hash = SHA-256(prevHash + canonicalJSON(body))` where body is every field except `prevHash` and `hash`.
 */
export interface LedgerEntry {
  /** 0-based position in the chain */
  index: number;
  ts: string;
  missionId: number | null;
  action: LedgerEntryAction;
  /** e.g. "executor" */
  source: string;
  missionObjective: string | null;
  verdictRef: LedgerEntryVerdictRef;
  /** Snapshot of the Queen FINAL_VERDICT trade plan at the moment this entry was written. */
  tradePlan: unknown | null;
  /** Action-specific payload (request/response, fills, errors). Secrets are scrubbed before write. */
  payload: unknown;
  prevHash: string;
  hash: string;
}

export interface LedgerPage {
  entries: LedgerEntry[];
  total: number;
}

export interface LedgerVerifyResult {
  ok: boolean;
  totalEntries: number;
  lastHash: string;
  brokeAtIndex: number | null;
  brokeAtReason: string | null;
  missionEntries?: number | null;
  missionFirstIndex?: number | null;
  missionLastIndex?: number | null;
  missionLastHash?: string | null;
}

export interface HealthStatus {
  status: string;
}

export type MissionStatusProperty =
  (typeof MissionStatusProperty)[keyof typeof MissionStatusProperty];

export const MissionStatusProperty = {
  pending: "pending",
  running: "running",
  awaiting_queen: "awaiting_queen",
  awaiting_intervention: "awaiting_intervention",
  locked: "locked",
  executing: "executing",
  completed: "completed",
  vetoed: "vetoed",
  aborted: "aborted",
} as const;

/**
 * Debate cadence. `scalp` caps at 2 cycles with minimal reasoning effort (~20s); `swing` uses the legacy 4-cycle pace with full reasoning (~3min).
 */
export type MissionSpeedMode =
  (typeof MissionSpeedMode)[keyof typeof MissionSpeedMode];

export const MissionSpeedMode = {
  scalp: "scalp",
  swing: "swing",
} as const;

export interface Mission {
  id: number;
  primeObjective: string;
  status: MissionStatusProperty;
  cycleCount: number;
  currentPhase?: string | null;
  thesisLock?: string | null;
  /** Hyperliquid perp symbol this mission is anchored to (uppercase). When set, agents receive a live market-context block and the chart panel renders. */
  targetSymbol?: string | null;
  /** Debate cadence. `scalp` caps at 2 cycles with minimal reasoning effort (~20s); `swing` uses the legacy 4-cycle pace with full reasoning (~3min). */
  speedMode?: MissionSpeedMode;
  /** Cumulative LLM spend on this mission in USD, summed across every Strategist/Adversary/Synthesizer/Observer/Queen/Executor call. gpt-5-mini priced at $0.25/M input + $2/M output (reasoning tokens billed as output). */
  costUsd: number;
  createdAt: string;
  updatedAt: string;
}

/**
 * Debate cadence. "scalp" (default) caps the run at 2 cycles with reasoning_effort=minimal so a confident thesis lands in ~20s, suitable for 1m–5m perp trading. "swing" preserves the slower 4-cycle thoroughness with no reasoning cap.
 */
export type CreateMissionBodySpeedMode =
  (typeof CreateMissionBodySpeedMode)[keyof typeof CreateMissionBodySpeedMode];

export const CreateMissionBodySpeedMode = {
  scalp: "scalp",
  swing: "swing",
} as const;

export interface CreateMissionBody {
  /** @minLength 10 */
  primeObjective: string;
  /** Optional Hyperliquid perp symbol (uppercase). Validated against the live perp universe. */
  targetSymbol?: string | null;
  /** Debate cadence. "scalp" (default) caps the run at 2 cycles with reasoning_effort=minimal so a confident thesis lands in ~20s, suitable for 1m–5m perp trading. "swing" preserves the slower 4-cycle thoroughness with no reasoning cap. */
  speedMode?: CreateMissionBodySpeedMode;
}

export interface MissionStatus {
  id: number;
  status: string;
  cycleCount: number;
  currentPhase?: string | null;
  lastPacketAt?: string | null;
}

export type ReasoningPacketAgentRole =
  (typeof ReasoningPacketAgentRole)[keyof typeof ReasoningPacketAgentRole];

export const ReasoningPacketAgentRole = {
  worker: "worker",
  observer: "observer",
  queen: "queen",
  executor: "executor",
} as const;

export type ChartAnnotationsMarkersItemKind =
  (typeof ChartAnnotationsMarkersItemKind)[keyof typeof ChartAnnotationsMarkersItemKind];

export const ChartAnnotationsMarkersItemKind = {
  entry: "entry",
  exit: "exit",
  stop: "stop",
  take_profit: "take_profit",
  signal: "signal",
  note: "note",
} as const;

export type ChartAnnotationsMarkersItemPosition =
  (typeof ChartAnnotationsMarkersItemPosition)[keyof typeof ChartAnnotationsMarkersItemPosition];

export const ChartAnnotationsMarkersItemPosition = {
  aboveBar: "aboveBar",
  belowBar: "belowBar",
  inBar: "inBar",
} as const;

export type ChartAnnotationsMarkersItemShape =
  (typeof ChartAnnotationsMarkersItemShape)[keyof typeof ChartAnnotationsMarkersItemShape];

export const ChartAnnotationsMarkersItemShape = {
  arrowUp: "arrowUp",
  arrowDown: "arrowDown",
  circle: "circle",
  square: "square",
} as const;

export type ChartAnnotationsMarkersItemColor =
  (typeof ChartAnnotationsMarkersItemColor)[keyof typeof ChartAnnotationsMarkersItemColor];

export const ChartAnnotationsMarkersItemColor = {
  cyan: "cyan",
  red: "red",
  amber: "amber",
  muted: "muted",
} as const;

export type ChartAnnotationsPriceLinesItemKind =
  (typeof ChartAnnotationsPriceLinesItemKind)[keyof typeof ChartAnnotationsPriceLinesItemKind];

export const ChartAnnotationsPriceLinesItemKind = {
  entry: "entry",
  stop: "stop",
  take_profit: "take_profit",
  level: "level",
} as const;

export type ChartAnnotationsPriceLinesItemColor =
  (typeof ChartAnnotationsPriceLinesItemColor)[keyof typeof ChartAnnotationsPriceLinesItemColor];

export const ChartAnnotationsPriceLinesItemColor = {
  cyan: "cyan",
  red: "red",
  amber: "amber",
  muted: "muted",
} as const;

export type ChartAnnotationsMarkersItem = {
  kind: ChartAnnotationsMarkersItemKind;
  timeUtcSec: number;
  position: ChartAnnotationsMarkersItemPosition;
  shape: ChartAnnotationsMarkersItemShape;
  color: ChartAnnotationsMarkersItemColor;
  text: string;
};

export type ChartAnnotationsPriceLinesItem = {
  kind: ChartAnnotationsPriceLinesItemKind;
  price: number;
  color: ChartAnnotationsPriceLinesItemColor;
  label: string;
};

export interface ChartAnnotations {
  markers: ChartAnnotationsMarkersItem[];
  priceLines: ChartAnnotationsPriceLinesItem[];
}

export interface ReasoningPacket {
  id: number;
  missionId: number;
  cycle: number;
  agentRole: ReasoningPacketAgentRole;
  reasoning: string;
  proposal?: string | null;
  verdict?: string | null;
  alignmentScore?: number | null;
  /** Chart annotations emitted by Synthesizer or Queen agents (markers + price lines for the candlestick chart). */
  annotations?: ChartAnnotations | null;
  /** Prompt tokens consumed by the LLM call that produced this packet. */
  tokensIn?: number | null;
  /** Completion tokens emitted by the LLM call (includes invisible reasoning tokens). */
  tokensOut?: number | null;
  /** Subset of tokensOut spent on hidden reasoning (gpt-5-mini extended-thinking). */
  reasoningTokens?: number | null;
  /** USD cost of the LLM call that produced this packet, computed from tokensIn/tokensOut at gpt-5-mini rates. */
  costUsd?: number | null;
  /** Wall-clock duration in milliseconds for the LLM call that produced this packet. Used to surface per-agent latency in the Reasoning Feed. */
  durationMs?: number | null;
  createdAt: string;
}

export type MissionRunStreamEventType =
  (typeof MissionRunStreamEventType)[keyof typeof MissionRunStreamEventType];

export const MissionRunStreamEventType = {
  worker_proposal: "worker_proposal",
  observer_audit: "observer_audit",
  queen_checkpoint: "queen_checkpoint",
  queen_guidance: "queen_guidance",
  cycle_complete: "cycle_complete",
  chart_annotation: "chart_annotation",
  cost_tick: "cost_tick",
  deadlock: "deadlock",
  done: "done",
  error: "error",
} as const;

export type ChartAnnotationEventDataSource =
  (typeof ChartAnnotationEventDataSource)[keyof typeof ChartAnnotationEventDataSource];

export const ChartAnnotationEventDataSource = {
  synthesizer: "synthesizer",
  queen: "queen",
} as const;

/**
 * Payload for `chart_annotation` events. Carries one cycle's worth of markers + price lines, sourced from Synthesizer or Queen.
 */
export interface ChartAnnotationEventData {
  packetId: number;
  missionId: number;
  cycle: number;
  source: ChartAnnotationEventDataSource;
  annotations: ChartAnnotations;
}

export type MissionRunStreamEventData =
  | ChartAnnotationEventData
  | { [key: string]: unknown };

/**
 * Discriminated union of events streamed from POST /missions/{id}/run.
Each SSE `data:` line is a JSON object of this shape. The `type`
field determines which fields are populated under `data`.
 */
export interface MissionRunStreamEvent {
  type: MissionRunStreamEventType;
  data: MissionRunStreamEventData;
}

export type VetoVetoedBy = (typeof VetoVetoedBy)[keyof typeof VetoVetoedBy];

export const VetoVetoedBy = {
  observer: "observer",
  queen: "queen",
} as const;

export interface Veto {
  id: number;
  missionId: number;
  cycle: number;
  vetoedBy: VetoVetoedBy;
  reason: string;
  proposalSummary: string;
  createdAt: string;
}

export interface QueenDecisionBody {
  reason: string;
  thesisLock?: string | null;
}

export interface QueenInterveneBody {
  /** @minLength 5 */
  guidance: string;
}

export type QueenDecisionDecision =
  (typeof QueenDecisionDecision)[keyof typeof QueenDecisionDecision];

export const QueenDecisionDecision = {
  approved: "approved",
  vetoed: "vetoed",
  aborted: "aborted",
} as const;

/**
 * The execution mode at the moment of approval. Null when the mission has no placeable structured trade plan.
 */
export type QueenDecisionExecutionMode =
  | (typeof QueenDecisionExecutionMode)[keyof typeof QueenDecisionExecutionMode]
  | null;

export const QueenDecisionExecutionMode = {
  paper: "paper",
  real: "real",
} as const;

export interface QueenDecision {
  missionId: number;
  decision: QueenDecisionDecision;
  reason: string;
  recordedAt: string;
  /** True when the server fired executeMission() in the background after this approval (paper-mode only). Real mode requires a manual EXECUTE TRADE NOW click. */
  autoExecuteScheduled?: boolean;
  /** The execution mode at the moment of approval. Null when the mission has no placeable structured trade plan. */
  executionMode?: QueenDecisionExecutionMode;
}

export interface Candle {
  /** UTC seconds (open time) */
  time: number;
  open: number;
  high: number;
  low: number;
  close: number;
  volume: number;
}

export interface CandleBatch {
  symbol: string;
  interval: string;
  candles: Candle[];
}

export interface MarketSnapshot {
  symbol: string;
  markPrice: number;
  oraclePrice: number;
  midPrice?: number | null;
  bid?: number | null;
  ask?: number | null;
  bidSize?: number | null;
  askSize?: number | null;
  spread?: number | null;
  spreadBps?: number | null;
  fundingRate: number;
  openInterest: number;
  prevDayPrice: number;
  dayVolumeUsd: number;
  changePct24h: number;
  /** Unix epoch milliseconds */
  timestamp: number;
}

export interface AuditSummary {
  recentPackets: ReasoningPacket[];
  recentVetoes: Veto[];
}

export interface AuditStats {
  totalMissions: number;
  activeMissions: number;
  totalPackets: number;
  totalVetoes: number;
  avgAlignmentScore?: number | null;
  vetoRate?: number | null;
}

export type GovernanceRuleSeverity =
  (typeof GovernanceRuleSeverity)[keyof typeof GovernanceRuleSeverity];

export const GovernanceRuleSeverity = {
  low: "low",
  medium: "medium",
  high: "high",
  critical: "critical",
} as const;

export interface GovernanceRule {
  id: number;
  name: string;
  description: string;
  severity: GovernanceRuleSeverity;
  active: boolean;
  createdAt: string;
}

export type CreateRuleBodySeverity =
  (typeof CreateRuleBodySeverity)[keyof typeof CreateRuleBodySeverity];

export const CreateRuleBodySeverity = {
  low: "low",
  medium: "medium",
  high: "high",
  critical: "critical",
} as const;

export interface CreateRuleBody {
  name: string;
  description: string;
  severity: CreateRuleBodySeverity;
}

/**
 * Operator-facing view of the execution config. The private key is never returned — `hasPrivateKey` only indicates whether one is stored.
 */
export interface ExecutionConfig {
  /** Hyperliquid wallet address (0x…40 hex chars). Public — safe to display. */
  walletAddress?: string | null;
  /** True if an encrypted private key is stored. Real-mode execution is only allowed when this is true. */
  hasPrivateKey: boolean;
  /** When true, all signed orders go to api.hyperliquid-testnet.xyz instead of mainnet. */
  useTestnet: boolean;
  /** When true, no signed orders are sent — fills are simulated against the live mark and persisted to the same execution_orders / execution_positions tables with paper=true. */
  paperMode: boolean;
  /** USD notional placed at entry. Size = notional / entry_price, rounded to the perp's szDecimals. */
  notionalPerTradeUsd: number;
  /** Hard cap. notionalPerTradeUsd may not exceed this. */
  maxNotionalUsd: number;
  /** Max number of OPEN positions across all missions. Execution refuses to place a new entry when this cap is hit. */
  maxConcurrentTrades: number;
  /** Cross-margin leverage to send with new orders. (Reserved — current implementation relies on account-level leverage.) */
  defaultLeverage: number;
  updatedAt: string;
}

/**
 * All fields optional. Omit a field to leave it unchanged. Send privateKey as empty string to clear a stored key.
 */
export interface UpdateExecutionConfigBody {
  /** @pattern ^0x[a-fA-F0-9]{40}$ */
  walletAddress?: string | null;
  /** 32-byte hex (with or without 0x prefix). Encrypted at rest with AES-256-GCM. Empty string clears. */
  privateKey?: string | null;
  useTestnet?: boolean;
  paperMode?: boolean;
  notionalPerTradeUsd?: number;
  maxNotionalUsd?: number;
  maxConcurrentTrades?: number;
  defaultLeverage?: number;
}

export type ExecutionOrderKind =
  (typeof ExecutionOrderKind)[keyof typeof ExecutionOrderKind];

export const ExecutionOrderKind = {
  entry: "entry",
  stop: "stop",
  tp1: "tp1",
  tp2: "tp2",
} as const;

export type ExecutionOrderSide =
  (typeof ExecutionOrderSide)[keyof typeof ExecutionOrderSide];

export const ExecutionOrderSide = {
  buy: "buy",
  sell: "sell",
} as const;

export type ExecutionOrderOrderType =
  (typeof ExecutionOrderOrderType)[keyof typeof ExecutionOrderOrderType];

export const ExecutionOrderOrderType = {
  market: "market",
  limit: "limit",
  trigger: "trigger",
} as const;

export type ExecutionOrderStatus =
  (typeof ExecutionOrderStatus)[keyof typeof ExecutionOrderStatus];

export const ExecutionOrderStatus = {
  pending: "pending",
  open: "open",
  filled: "filled",
  cancelled: "cancelled",
  rejected: "rejected",
  error: "error",
} as const;

export interface ExecutionOrder {
  id: number;
  missionId: number;
  kind: ExecutionOrderKind;
  side: ExecutionOrderSide;
  reduceOnly: boolean;
  orderType: ExecutionOrderOrderType;
  triggerPx?: number | null;
  limitPx?: number | null;
  sz: number;
  /** Hyperliquid order id (oid) returned by the exchange. Null for paper rows and for legs that errored before placement. */
  hlOrderId?: string | null;
  status: ExecutionOrderStatus;
  filledSz: number;
  avgFillPx?: number | null;
  errorMessage?: string | null;
  paper: boolean;
  createdAt: string;
  updatedAt: string;
}

export type ExecutionPositionSide =
  (typeof ExecutionPositionSide)[keyof typeof ExecutionPositionSide];

export const ExecutionPositionSide = {
  long: "long",
  short: "short",
} as const;

export type ExecutionPositionStatus =
  (typeof ExecutionPositionStatus)[keyof typeof ExecutionPositionStatus];

export const ExecutionPositionStatus = {
  open: "open",
  closed: "closed",
  killed: "killed",
} as const;

export interface ExecutionPosition {
  id: number;
  missionId: number;
  symbol: string;
  side: ExecutionPositionSide;
  sz: number;
  entryPx: number;
  unrealizedPnlUsd: number;
  realizedPnlUsd: number;
  status: ExecutionPositionStatus;
  paper: boolean;
  openedAt: string;
  closedAt?: string | null;
  lastSyncedAt: string;
}

export type ExecutionPlacedLegKind =
  (typeof ExecutionPlacedLegKind)[keyof typeof ExecutionPlacedLegKind];

export const ExecutionPlacedLegKind = {
  entry: "entry",
  stop: "stop",
  tp1: "tp1",
  tp2: "tp2",
} as const;

export type ExecutionPlacedLegSide =
  (typeof ExecutionPlacedLegSide)[keyof typeof ExecutionPlacedLegSide];

export const ExecutionPlacedLegSide = {
  buy: "buy",
  sell: "sell",
} as const;

export type ExecutionPlacedLegOrderType =
  (typeof ExecutionPlacedLegOrderType)[keyof typeof ExecutionPlacedLegOrderType];

export const ExecutionPlacedLegOrderType = {
  market: "market",
  trigger: "trigger",
} as const;

export type ExecutionPlacedLegStatus =
  (typeof ExecutionPlacedLegStatus)[keyof typeof ExecutionPlacedLegStatus];

export const ExecutionPlacedLegStatus = {
  open: "open",
  filled: "filled",
  rejected: "rejected",
  error: "error",
} as const;

export interface ExecutionPlacedLeg {
  kind: ExecutionPlacedLegKind;
  side: ExecutionPlacedLegSide;
  reduceOnly: boolean;
  orderType: ExecutionPlacedLegOrderType;
  triggerPx?: number | null;
  limitPx?: number | null;
  sz: number;
  hlOrderId?: string | null;
  status: ExecutionPlacedLegStatus;
  filledSz: number;
  avgFillPx?: number | null;
  errorMessage?: string | null;
}

export type ExecutionPlacementResultPositionSide =
  (typeof ExecutionPlacementResultPositionSide)[keyof typeof ExecutionPlacementResultPositionSide];

export const ExecutionPlacementResultPositionSide = {
  long: "long",
  short: "short",
} as const;

export type ExecutionPlacementResultPosition = {
  symbol: string;
  side: ExecutionPlacementResultPositionSide;
  sz: number;
  entryPx: number;
} | null;

export interface ExecutionPlacementResult {
  /** True if executed in paper mode (no signed orders sent). */
  paper: boolean;
  legs: ExecutionPlacedLeg[];
  position?: ExecutionPlacementResultPosition;
}

export interface ExecutionKillResult {
  /** Number of resting orders successfully cancelled (real-mode count from Hyperliquid; paper-mode = the row count). */
  cancelled: number;
  /** True if an open position was flat-closed. */
  closed: boolean;
  /** Mark price (paper) or actual fill price (real) at which the position was closed. */
  closedPx?: number | null;
  errors: string[];
}

export interface MissionExecutionSnapshot {
  orders: ExecutionOrder[];
  position?: ExecutionPosition | null;
}

export type ListMarketSymbols200 = {
  symbols: string[];
};

export type GetMarketSnapshot400 = {
  error?: string;
};

export type GetMarketSnapshot502 = {
  error?: string;
};

export type GetMarketCandlesParams = {
  interval?: GetMarketCandlesInterval;
  /**
   * @minimum 1
   * @maximum 1000
   */
  limit?: number;
};

export type GetMarketCandlesInterval =
  (typeof GetMarketCandlesInterval)[keyof typeof GetMarketCandlesInterval];

export const GetMarketCandlesInterval = {
  "1m": "1m",
  "5m": "5m",
  "15m": "15m",
  "1h": "1h",
  "4h": "4h",
  "1d": "1d",
} as const;

export type GetMarketCandles400 = {
  error?: string;
};

export type GetMarketCandles502 = {
  error?: string;
};

export type StreamMarketSnapshot400 = {
  error?: string;
};

export type StreamMarketSnapshot502 = {
  error?: string;
};

export type ExecuteMission400 = {
  error?: string;
};

export type ExecuteMission409 = {
  error?: string;
};

export type ListLedgerEntriesParams = {
  missionId?: number;
  action?: ListLedgerEntriesAction;
  after?: string;
  before?: string;
  /**
   * @minimum 1
   * @maximum 5000
   */
  limit?: number;
  /**
   * @minimum 0
   */
  offset?: number;
};

export type ListLedgerEntriesAction =
  (typeof ListLedgerEntriesAction)[keyof typeof ListLedgerEntriesAction];

export const ListLedgerEntriesAction = {
  submit: "submit",
  fill: "fill",
  cancel: "cancel",
  kill: "kill",
  close: "close",
  error: "error",
} as const;
